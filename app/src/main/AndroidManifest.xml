<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ingenieriasoftware.sefitness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeFitness">
        <activity android:name=".SolicitarBajaActivity"></activity>
        <activity android:name=".CitasActivity" />
        <activity android:name=".GastoCaloricoActivity" />
        <activity android:name=".RutinasActivity" />
        <activity android:name=".DietasActivity" />
        <activity android:name=".AvancesActivity" />
        <activity android:name=".PerfilUsuarioActivity" />
        <activity android:name=".Registro" />
        <activity android:name=".InicioClienteActivity" />
        <activity android:name=".RecuperarContrasenyaActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>